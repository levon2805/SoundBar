<Window x:Class="SoundBar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SoundBar" Height="500" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="#2D2D30" 
            CornerRadius="15" 
            BorderBrush="#444444" 
            BorderThickness="1"
            Padding="20">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,10">

                <Grid Margin="0,0,0,15">
                    <TextBlock Text="SoundBar" 
                               Foreground="White" 
                               FontSize="18" 
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>

                    <Button Content="✕" 
                            HorizontalAlignment="Right"
                            Width="30" Height="30"
                            Background="Transparent"
                            Foreground="#AAAAAA"
                            BorderThickness="0"
                            FontSize="14"
                            Cursor="Hand"
                            Click="CloseButton_Click"/>
                </Grid>

                <TextBlock Text="Master Volume" 
                           Foreground="#DDDDDD" 
                           FontSize="14" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <Slider Grid.Column="0"
                            Minimum="0.0" 
                            Maximum="1.0" 
                            Value="{Binding MasterVolume}"
                            Margin="0,0,10,0"
                            IsMoveToPointEnabled="True"/>

                    <TextBlock Grid.Column="1" 
                               Foreground="#AAAAAA"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right">
                        <TextBlock.Text>
                            <Binding Path="MasterVolume" StringFormat="P0"/>
                        </TextBlock.Text>
                    </TextBlock>
                </Grid>

                <Separator Background="#555555" Margin="0,20,0,10"/>

                <TextBlock Text="Active Apps" 
                           Foreground="#DDDDDD" 
                           FontSize="14" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Apps}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}" 
                                           Foreground="White"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>

                                <Slider Grid.Column="1" 
                                        Minimum="0.0" 
                                        Maximum="1.0" 
                                        Value="{Binding Volume}"
                                        Margin="10,0"
                                        IsMoveToPointEnabled="True"/>

                                <TextBlock Grid.Column="2" 
                                           Foreground="#AAAAAA"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right">
                                    <TextBlock.Text>
                                        <Binding Path="Volume" StringFormat="P0"/>
                                    </TextBlock.Text>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>