<Window x:Class="SoundBar.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" 
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
            BorderThickness="1">
        <Border.Resources>
            <ResourceDictionary>
                <ResourceDictionary.ThemeDictionaries>
                    <ResourceDictionary x:Key="Light">
                        <SolidColorBrush x:Key="DimmingOverlayBrush" Color="#D8EAEAEA" />
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="Dark">
                        <SolidColorBrush x:Key="DimmingOverlayBrush" Color="#D8202020" />
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="Default">
                        <SolidColorBrush x:Key="DimmingOverlayBrush" Color="#D8202020" />
                    </ResourceDictionary>
                </ResourceDictionary.ThemeDictionaries>
            </ResourceDictionary>
        </Border.Resources>

        <Grid>
            <!-- Background Image Layer -->
            <Image Stretch="UniformToFill" Source="{x:Bind ViewModel.BackgroundImage, Mode=OneWay}" />
            <!-- Dimming Overlay Layer -->
            <Border Background="{ThemeResource DimmingOverlayBrush}" />

            <!-- Application Content -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid x:Name="TitleBarGrid" Grid.Row="0" Margin="0,0,0,15" Padding="20,20,20,0" Background="Transparent" Canvas.ZIndex="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="SoundBar" 
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                           VerticalAlignment="Center" 
                           Margin="0"
                           FontSize="20"
                           FontWeight="Bold"/>

                <Button x:Name="UpdateButton"
                        Grid.Column="1"
                        Content="&#xE896;" 
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        Width="36" Height="36"
                        Padding="0" MinWidth="0" MinHeight="0"
                        Background="#1F6B2C"
                        Foreground="White"
                        BorderThickness="0"
                        CornerRadius="4"
                        FontSize="14"
                        Margin="0,0,10,0"
                        Click="UpdateBanner_Click"
                        Visibility="{x:Bind ViewModel.UpdateBannerVisibility, Mode=OneWay}"
                        ToolTipService.ToolTip="{x:Bind ViewModel.UpdateBannerText, Mode=OneWay}"/>

                <Button x:Name="SettingsButton"
                        Grid.Column="2"
                        Content="&#xE713;" 
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        Width="36" Height="36"
                        Padding="0" MinWidth="0" MinHeight="0"
                        Background="Transparent"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        BorderThickness="0"
                        FontSize="14"
                        Margin="0,0,5,0"
                        Click="SettingsButton_Click"
                        ToolTipService.ToolTip="Settings"/>

                <Button x:Name="PinButton"
                        Grid.Column="3"
                        Content="&#xE718;" 
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        Width="36" Height="36"
                        Padding="0" MinWidth="0" MinHeight="0"
                        Background="Transparent"
                        Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                        BorderThickness="0"
                        FontSize="16"
                        Margin="0,0,5,0"
                        Click="PinButton_Click"
                        ToolTipService.ToolTip="Always on Top"/>

                <Button x:Name="CompanionButton"
                        Grid.Column="4"
                        Content="&#xE8EA;" 
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        Width="36" Height="36"
                        Padding="0" MinWidth="0" MinHeight="0"
                        Background="Transparent"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        BorderThickness="0"
                        FontSize="16"
                        Margin="0,0,5,0"
                        Click="CompanionButton_Click"
                        ToolTipService.ToolTip="Mobile Companion"/>

                <ToggleButton x:Name="DndToggleButton"
                              Grid.Column="5"
                              Content="&#xE708;" 
                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                              Width="36" Height="36"
                              Padding="0" MinWidth="0" MinHeight="0"
                              Background="Transparent"
                              Foreground="{ThemeResource TextFillColorDisabledBrush}"
                              BorderThickness="0"
                              FontSize="14"
                              Margin="0,0,5,0"
                              IsChecked="{x:Bind ViewModel.IsDoNotDisturbEnabled, Mode=TwoWay}"
                              Checked="DndToggleButton_Changed"
                              Unchecked="DndToggleButton_Changed"
                              ToolTipService.ToolTip="Do Not Disturb (Mute System Sounds)"/>

                <Button x:Name="MinimizeButton"
                        Grid.Column="5"
                        Content="&#xE921;" 
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        Width="36" Height="36"
                        Padding="0" MinWidth="0" MinHeight="0"
                        Background="Transparent"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        BorderThickness="0"
                        FontSize="14"
                        Click="MinimizeButton_Click"/>

                <Button Grid.Column="6"
                        Content="&#xE711;" 
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        Width="36" Height="36"
                        Padding="0" MinWidth="0" MinHeight="0"
                        Background="Transparent"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        BorderThickness="0"
                        FontSize="14"
                        Click="CloseButton_Click"/>
            </Grid>

            <!-- Main Content -->
            <Grid x:Name="MainContentGrid" Grid.Row="1" Margin="20,0,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- MIXER VIEW -->
                <Grid Grid.Row="0" Grid.RowSpan="2" Visibility="{x:Bind ViewModel.MixerViewVisibility, Mode=OneWay}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0">

                    <Border Background="#66FF0000"
                            BorderBrush="#FF5555"
                            BorderThickness="1"
                            CornerRadius="4"
                            Margin="0,0,0,15"
                            Padding="10"
                            Visibility="{x:Bind ViewModel.LoudnessWarningVisibility, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock FontFamily="{ThemeResource SymbolThemeFontFamily}" Text="&#xEA39;" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" FontSize="16"/>
                            <TextBlock Text="Volume has been very high for a while. Protect your hearing!" Foreground="{ThemeResource TextFillColorPrimaryBrush}" VerticalAlignment="Center" TextWrapping="Wrap" Width="200"/>
                            <Button Content="Dismiss" Background="{ThemeResource ControlFillColorSecondaryBrush}" Foreground="{ThemeResource TextFillColorPrimaryBrush}" BorderThickness="0" VerticalAlignment="Center" Click="DismissLoudnessWarning_Click"/>
                        </StackPanel>
                    </Border>

                    <!-- Combined I/O Device Strip -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="OutputColumnDef" Width="*"/>
                            <ColumnDefinition x:Name="MuteButtonColumnDef" Width="Auto"/>
                            <ColumnDefinition x:Name="InputColumnDef" Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Output Device Picker -->
                        <ComboBox x:Name="DeviceComboBox"
                                  Grid.Column="0"
                                  HorizontalAlignment="Stretch"
                                  ItemsSource="{Binding AudioDevices}"
                                  SelectedItem="{Binding SelectedAudioDevice, Mode=TwoWay}"
                                  DisplayMemberPath="Name"
                                  PlaceholderText="No output devices detected"
                                  Background="{ThemeResource ControlFillColorDefaultBrush}"
                                  BorderThickness="1"
                                  BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Margin="0,0,6,0"
                                  Visibility="{x:Bind ViewModel.OutputDeviceVisibility, Mode=OneWay}"/>

                        <!-- Mic Mute Toggle Button -->
                        <Button x:Name="MicMuteButton"
                                Grid.Column="1"
                                Margin="0,0,6,0"
                                Width="36" Height="32"
                                Padding="0" MinWidth="0" MinHeight="0"
                                Background="{ThemeResource ControlFillColorDefaultBrush}"
                                BorderThickness="1"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                CornerRadius="4"
                                Click="MicMuteButton_Click"
                                Visibility="{x:Bind ViewModel.InputDeviceVisibility, Mode=OneWay}"
                                ToolTipService.ToolTip="Mute/Unmute Microphone">
                            <TextBlock x:Name="MicMuteIcon"
                                       Text="{x:Bind ViewModel.InputMuteIcon, Mode=OneWay}"
                                       FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                       FontSize="14"/>
                        </Button>

                        <!-- Input Device Picker -->
                        <ComboBox x:Name="InputDeviceComboBox"
                                  Grid.Column="2"
                                  HorizontalAlignment="Stretch"
                                  MinWidth="100"
                                  ItemsSource="{Binding InputDevices}"
                                  SelectedItem="{Binding SelectedInputDevice, Mode=TwoWay}"
                                  DisplayMemberPath="Name"
                                  PlaceholderText="No input devices detected"
                                  Background="{ThemeResource ControlFillColorDefaultBrush}"
                                  BorderThickness="1"
                                  BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  Visibility="{x:Bind ViewModel.InputDeviceVisibility, Mode=OneWay}"/>
                    </Grid>

                    <!-- Master Volume -->
                    <StackPanel Visibility="{x:Bind ViewModel.MasterVolumeVisibility, Mode=OneWay}">
                        <TextBlock Text="Master Volume" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                   FontSize="14" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,5"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <Slider Grid.Column="0"
                                    Minimum="0" 
                                    Maximum="100" 
                                    Value="{Binding MasterVolumePercentage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Margin="0,0,10,0"/>

                            <TextBlock Grid.Column="1" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right">
                                <Run Text="{Binding MasterVolumePercentage}" /><Run Text="%" />
                            </TextBlock>
                        </Grid>
                    </StackPanel>

                    <Border Height="1" Background="{ThemeResource ControlStrokeColorDefaultBrush}" Margin="0,20,0,10"/>

                    <!-- Active Apps Header + List -->
                    <StackPanel Visibility="{x:Bind ViewModel.ActiveAppsVisibility, Mode=OneWay}">
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="Active Apps" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>

                            <Button Click="RefreshApps_Click"
                                    Background="Transparent" 
                                    BorderBrush="Transparent"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    ToolTipService.ToolTip="Force refresh audio sessions"
                                    Padding="4">
                                <FontIcon Glyph="&#xE72C;" FontSize="12"/>
                            </Button>
                        </Grid>
                    </StackPanel>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Visibility="{x:Bind ViewModel.ActiveAppsVisibility, Mode=OneWay}">
                    <ItemsControl ItemsSource="{Binding Apps}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0" 
                                            Content="&#xE74D;" 
                                            FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                                            Background="Transparent" 
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                                            BorderThickness="0" 
                                            Click="HideAppButton_Click" 
                                            ToolTipService.ToolTip="Hide App" 
                                            Padding="0" MinWidth="20" Height="20"
                                            VerticalAlignment="Center"
                                            Margin="0,0,5,0" />

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Image Source="{Binding AppIcon}" Width="20" Height="20" Margin="0,0,8,0" Stretch="Uniform"/>
                                        <TextBox Text="{Binding Name, Mode=TwoWay}" 
                                                 Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 VerticalAlignment="Center"
                                                 MinHeight="0"
                                                 MinWidth="0"
                                                 Padding="0"
                                                 Margin="0"
                                                 KeyDown="TextBox_KeyDown"
                                                 ToolTipService.ToolTip="Click to rename"/>
                                    </StackPanel>

                                    <Slider Grid.Column="2" 
                                            Minimum="0" 
                                            Maximum="100" 
                                            Value="{Binding VolumePercentage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Margin="10,0"/>

                                    <TextBlock Grid.Column="3" 
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Right">
                                        <Run Text="{Binding VolumePercentage}" /><Run Text="%" />
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                </Grid>

                <!-- MUSIC PLAYER VIEW -->
                <Grid Grid.Row="0" Grid.RowSpan="2" Visibility="{x:Bind ViewModel.MusicPlayerViewVisibility, Mode=OneWay}" Padding="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Album Art -->
                    <Viewbox Grid.Row="0" Margin="10" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="280">
                        <Grid>
                            <Border CornerRadius="12" Background="{ThemeResource LayerFillColorAltBrush}" Width="250" Height="250">
                                <Image Source="{x:Bind ViewModel.CurrentSongThumbnail, Mode=OneWay}" Stretch="UniformToFill">
                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="No Artwork Available" />
                                    </ToolTipService.ToolTip>
                                </Image>
                            </Border>
                            <!-- Fallback Icon behind image -->
                            <TextBlock FontFamily="{ThemeResource SymbolThemeFontFamily}" Text="&#xE189;" FontSize="64" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{x:Bind ViewModel.FallbackIconVisibility, Mode=OneWay}" />
                        </Grid>
                    </Viewbox>

                    <!-- Title -->
                    <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.CurrentSongTitle, Mode=OneWay}" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,0,0,5"/>

                    <!-- Artist -->
                    <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.CurrentSongArtist, Mode=OneWay}" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,0,0,15"/>

                    <!-- Scrub Bar -->
                    <Grid Grid.Row="3" Margin="20,0,20,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{x:Bind ViewModel.CurrentSongPositionText, Mode=OneWay}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0"/>
                        
                        <Slider x:Name="SongPositionSlider"
                                Grid.Column="1" 
                                Minimum="0" 
                                Maximum="{x:Bind ViewModel.CurrentSongDurationSeconds, Mode=OneWay}" 
                                Value="{x:Bind ViewModel.CurrentSongPositionSeconds, Mode=TwoWay}"
                                ThumbToolTipValueConverter="{StaticResource TimeSpanConverter}"
                                PointerCaptureLost="SongPositionSlider_PointerCaptureLost"
                                VerticalAlignment="Center" />

                        <TextBlock Grid.Column="2" Text="{x:Bind ViewModel.CurrentSongDurationText, Mode=OneWay}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"/>
                    </Grid>
                </Grid>

                <!-- COMPANION VIEW -->
                <Grid Grid.Row="0" Grid.RowSpan="2" Visibility="{x:Bind ViewModel.CompanionViewVisibility, Mode=OneWay}" Padding="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Mobile Companion" 
                               FontSize="20" FontWeight="Bold" 
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                               HorizontalAlignment="Center" Margin="0,0,0,10"/>

                    <!-- OFF STATE: Power Button -->
                    <Grid Grid.Row="1" Visibility="{x:Bind ViewModel.CompanionPowerButtonVisibility, Mode=OneWay}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                            <TextBlock Text="Server Offline" 
                                       FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                       HorizontalAlignment="Center"/>
                            
                            <!-- Huge Power Button -->
                            <ToggleButton IsChecked="{x:Bind ViewModel.EnableCompanionServer, Mode=TwoWay}"
                                          Width="120" Height="120" CornerRadius="60"
                                          Background="{ThemeResource ControlFillColorDefaultBrush}"
                                          BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                          BorderThickness="2"
                                          HorizontalAlignment="Center">
                                <TextBlock Text="&#xE7E8;" 
                                           FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                                           FontSize="50" 
                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </ToggleButton>
                            
                            <TextBlock Text="Click to power on" 
                                       FontSize="14" Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <!-- ON STATE: QR Code & Info -->
                    <Grid Grid.Row="1" Visibility="{x:Bind ViewModel.CompanionActiveUiVisibility, Mode=OneWay}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="6" Padding="0,5">
                                
                                <TextBlock Text="Scan to connect" 
                                           FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                           HorizontalAlignment="Center"/>

                                <!-- Dynamic QR using custom converter -->
                                <Image Width="120" Height="120" Stretch="Uniform" HorizontalAlignment="Center">
                                    <Image.Source>
                                        <BitmapImage UriSource="{x:Bind ViewModel.CompanionQrUrl, Mode=OneWay}" />
                                    </Image.Source>
                                </Image>

                                <TextBlock Text="{x:Bind ViewModel.CompanionPairingCode, Mode=OneWay}" 
                                           FontSize="32" FontWeight="Bold" 
                                           CharacterSpacing="200"
                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                                           HorizontalAlignment="Center" Margin="0,2,0,2"/>

                                <TextBlock Text="{x:Bind ViewModel.CompanionClientText, Mode=OneWay}" 
                                           FontSize="13" Foreground="{ThemeResource SystemFillColorSuccessBrush}" 
                                           HorizontalAlignment="Center" FontWeight="SemiBold"/>

                                <TextBlock Text="{x:Bind ViewModel.CompanionServerUrl, Mode=OneWay}" 
                                           FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                                           HorizontalAlignment="Center" IsTextSelectionEnabled="True"/>

                                <Button Content="Turn Off Server"
                                        Margin="0,8,0,0" Padding="12,4"
                                        HorizontalAlignment="Center"
                                        Click="CompanionPowerOff_Click"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>


                <!-- Media Controls -->
                <StackPanel Grid.Row="2" Margin="0,10,0,-10" Visibility="{x:Bind ViewModel.MediaControlsVisibility, Mode=OneWay}">
                    <Border Height="1" Background="{ThemeResource ControlStrokeColorDefaultBrush}" Margin="0,0,0,8"/>
                    
                    <Grid>
                        <!-- Music Player Mode Toggle -->
                        <Button HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="&#xE8D6;" 
                                FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                                Background="Transparent" 
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                                BorderThickness="0" 
                                FontSize="18"
                                Width="36" Height="36"
                                Padding="0" MinWidth="0" MinHeight="0"
                                Click="ToggleMusicPlayerMode_Click" 
                                ToolTipService.ToolTip="Toggle Music Player View" />

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                        <!-- Previous -->
                        <Button Content="&#xE892;" 
                                FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                                Background="{ThemeResource ControlFillColorSecondaryBrush}" 
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                BorderThickness="1"
                                BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" 
                                CornerRadius="18"
                                FontSize="14"
                                Width="36" Height="36"
                                Padding="0" MinWidth="0" MinHeight="0"
                                Click="MediaPrevious_Click" 
                                ToolTipService.ToolTip="Previous Track" />

                        <!-- Play/Pause -->
                        <Button Background="{ThemeResource ControlFillColorSecondaryBrush}" 
                                BorderThickness="1"
                                BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" 
                                CornerRadius="18"
                                Width="36" Height="36"
                                Padding="0" MinWidth="0" MinHeight="0"
                                Click="MediaPlayPause_Click" 
                                ToolTipService.ToolTip="Play / Pause">
                            <StackPanel Orientation="Horizontal" Spacing="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock FontFamily="{ThemeResource SymbolThemeFontFamily}" Text="&#xE768;" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock FontFamily="{ThemeResource SymbolThemeFontFamily}" Text="&#xE769;" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </Button>

                        <!-- Next -->
                        <Button Content="&#xE893;" 
                                FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                                Background="{ThemeResource ControlFillColorSecondaryBrush}" 
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                BorderThickness="1"
                                BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" 
                                CornerRadius="18"
                                FontSize="14"
                                Width="36" Height="36"
                                Padding="0" MinWidth="0" MinHeight="0"
                                Click="MediaNext_Click" 
                                ToolTipService.ToolTip="Next Track" />

                        <!-- Mute -->
                        <Button Content="&#xE74F;" 
                                FontFamily="{ThemeResource SymbolThemeFontFamily}" 
                                Background="{ThemeResource ControlFillColorSecondaryBrush}" 
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                BorderThickness="1"
                                BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" 
                                CornerRadius="18"
                                FontSize="14"
                                Width="36" Height="36"
                                Padding="0" MinWidth="0" MinHeight="0"
                                Click="MediaMute_Click" 
                                ToolTipService.ToolTip="Mute System" />
                    </StackPanel>
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Settings Content -->
            <Grid x:Name="SettingsContentGrid" Grid.Row="1" Visibility="Collapsed" Margin="20,0,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                    <Button Content="&#xE72B;" 
                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                            Background="Transparent"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            BorderThickness="0"
                            Padding="5"
                            FontSize="18"
                            Margin="0,0,10,0"
                            Click="CloseSettingsButton_Click"/>
                    <TextBlock Text="Settings" 
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                               FontSize="18" 
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                    <StackPanel x:Name="DynamicSettingsStackPanel" Spacing="10">
                        <!-- Theme Settings -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="Appearance" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Choose whether the app should follow your system settings or force Light/Dark mode." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <ComboBox HorizontalAlignment="Stretch"
                                          ItemsSource="{Binding AvailableThemes}"
                                          SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"
                                          Background="{ThemeResource ControlFillColorDefaultBrush}"
                                          BorderThickness="1"
                                          BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                          Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                                          Margin="0,0,0,15"/>

                                <TextBlock Text="Highlights the active application so you know which volume you are controlling via hotkeys." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <ToggleSwitch Header="Active App Highlight" 
                                              IsOn="{Binding EnableFocusHighlight, Mode=TwoWay}" 
                                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </Expander>

                        <!-- Hearing Protection -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="Hearing Protection" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Shows a warning if your master volume stays above 85% for an extended time." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <ToggleSwitch Header="Excessive Loudness Warning" 
                                              IsOn="{x:Bind ViewModel.IsLoudnessWarningEnabled, Mode=TwoWay}" 
                                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </Expander>

                        <!-- Custom Background -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="Custom Background" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Place a .jpg or .png image in the backgrounds folder to personalise your SoundBar." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="Open Folder" HorizontalAlignment="Stretch" Background="{ThemeResource ControlFillColorDefaultBrush}" Foreground="{ThemeResource TextFillColorPrimaryBrush}" BorderThickness="0" Click="OpenBackgroundFolder_Click"/>
                                    <Button Grid.Column="2" Content="Reload" HorizontalAlignment="Stretch" Background="{ThemeResource ControlFillColorDefaultBrush}" Foreground="{ThemeResource TextFillColorPrimaryBrush}" BorderThickness="0" Click="ReloadBackground_Click"/>
                                </Grid>
                            </StackPanel>
                        </Expander>

                        <!-- Hidden Apps -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="Hidden Apps" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Apps that you have manually hidden from the mixer." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <ItemsControl ItemsSource="{Binding HiddenApps}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,5">
                                                <TextBlock Text="{Binding}" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                                <Button Content="Unhide" 
                                                        Click="UnhideAppButton_Click" 
                                                        HorizontalAlignment="Right" 
                                                        Background="{ThemeResource ControlFillColorDefaultBrush}" 
                                                        Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                                                        BorderThickness="0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Expander>

                        <!-- Background Apps Second -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="Background Apps" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Apps that are running in the background and not outputting audio yet." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <ItemsControl ItemsSource="{Binding SystemBackgroundApps}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,5">
                                                <TextBlock Text="{Binding}" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorPrimaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="180" HorizontalAlignment="Left" />
                                                <Button Content="Allow" 
                                                        Click="AllowBackgroundAppButton_Click" 
                                                        HorizontalAlignment="Right" 
                                                        Background="{ThemeResource ControlFillColorDefaultBrush}" 
                                                        Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                                                        BorderThickness="0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Expander>

                        <!-- Media Controls Settings -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="Media Controls" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel>
                                <TextBlock Text="Shows global media controls at the bottom of the application (Previous, Play/Pause, Next, Mute)." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <ToggleSwitch Header="Show Media Controls" 
                                              IsOn="{x:Bind ViewModel.ShowMediaControls, Mode=TwoWay}" 
                                              Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </Expander>

                        <!-- System Integration Settings -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="System Integration" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel Spacing="15">
                                <StackPanel>
                                    <TextBlock Text="Automatically start SoundBar in the background when you log into Windows." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                    <ToggleSwitch Header="Run at Startup" 
                                                  IsOn="{x:Bind ViewModel.RunAtStartup, Mode=TwoWay}" 
                                                  Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Place a fresh shortcut to SoundBar directly on your Desktop." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                    <Button Content="Create Desktop Shortcut" 
                                            Click="{x:Bind ViewModel.CreateDesktopShortcut}" 
                                            Background="{ThemeResource ControlFillColorDefaultBrush}" 
                                            Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                                            BorderThickness="1"
                                            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Expander>

                        <!-- Advanced Configuration -->
                        <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                            <Expander.Header>
                                <TextBlock Text="Advanced Configuration" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="14" FontWeight="SemiBold"/>
                            </Expander.Header>
                            <StackPanel Spacing="15">
                                <StackPanel>
                                    <TextBlock Text="Modify hidden settings directly via the config file. (e.g. CompanionServerPort). A restart of the application is required to apply changes." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                    <Button Content="Open config.json" 
                                            Click="{x:Bind ViewModel.OpenSettingsFile}" 
                                            Background="{ThemeResource ControlFillColorDefaultBrush}" 
                                            Foreground="{ThemeResource TextFillColorPrimaryBrush}" 
                                            BorderThickness="1"
                                            BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- App Version (Global) -->
            <TextBlock Grid.Row="1"
                       Text="{x:Bind ViewModel.AppVersionText, Mode=OneWay}" 
                       Foreground="{ThemeResource TextFillColorDisabledBrush}" 
                       FontSize="10" 
                       HorizontalAlignment="Right" 
                       VerticalAlignment="Bottom"
                       Margin="0,0,10,5"/>
            </Grid>
        </Grid>
    </Border>
</Window>
